[pytest]
testpaths = tests src
python_files = test_*.py
addopts = -v --tb=short --strict-markers

markers =
    ros: tests that require ROS2 runtime (rclpy.init)
    serial: tests that require serial port or mock serial
    slow: tests that take longer than 1 second
