# 硬件接线与设备识别

本文档说明当前硬件清单、串口设备识别机制和常见问题排查。

## 1. 硬件清单

| 设备 | 型号 | 接口 | 设备名 |
|------|------|------|--------|
| 激光雷达 | YDLIDAR X2 | USB-UART (CP210x) | `/dev/ydlidar`（udev 规则）或 `/dev/ttyUSB*` |
| 底盘控制板 | STM32F407 | Raspberry Pi GPIO UART | `/dev/serial0`（通常指向 `/dev/ttyAMA0`） |
| IMU | BMI270 | STM32 侧 SPI | `IMU_STATUS` 转为 `/imu/observation`，状态估计发布 `/imu/data` |
| 电机 | JGB37-520 × 2 | TB67H450FNG 驱动 | — |
| 编码器 | AB 相霍尔 × 2 | TIM 编码器模式 | — |

不属于当前硬件的设备：

- Astra Pro 深度摄像头 — 属于后续 AI 扩展（Phase 7），当前激光 SLAM 主链路不包含摄像头

## 2. udev 规则配置

`launch_scripts/setup_lidar_permissions.sh` 完成以下配置：

1. 将 `robot` 用户加入 `dialout` 组（串口访问权限）
2. 写入 udev 规则 `/etc/udev/rules.d/99-ydlidar.rules`，为 CP210x 芯片创建稳定设备名 `/dev/ydlidar`

udev 规则内容：

```text
SUBSYSTEM=="tty", ATTRS{idVendor}=="10c4", ATTRS{idProduct}=="ea60", SYMLINK+="ydlidar", GROUP="dialout", MODE="0660"
```

执行方式：

```bash
sudo /home/robot/ros2_ws/launch_scripts/setup_lidar_permissions.sh
```

执行后需重新登录或重启，使 `dialout` 组成员变更生效。

## 3. 串口权限问题排查

当雷达驱动报 `Error, cannot bind to the specified [serial port]` 时，按以下步骤排查：

```bash
# 检查设备是否存在
ls -la /dev/ttyUSB* /dev/ydlidar 2>/dev/null

# 检查设备权限和所属组
ls -la /dev/ttyUSB0
# 期望: crw-rw---- 1 root dialout

# 检查当前用户组
groups
# 期望包含 dialout

# 如果不在 dialout 组
sudo usermod -aG dialout robot
# 重新登录后生效
```

临时测试（重启后失效）：

```bash
sudo chmod 666 /dev/ttyUSB0
```

## 4. 自动探测机制

### 底盘串口检查

`stm32_robot_bridge` 默认使用 `/dev/serial0`，不再把 USB 串口自动探测作为正式路径。`detect_base_port.sh --probe` 只做被动检查：

1. 优先检查 `ROBOT_BASE_PORT_HINT`，否则检查 `/dev/serial0`。
2. 确认设备存在且可读写。
3. 被动读取串口流，等待下位机主动上报的 v3 92B STATUS；v2 STATUS 不会被误判为兼容。
4. 不向下位机发送状态请求。

### 雷达串口探测

`detect_lidar_port.sh` 在现有串口中排除已知底盘端口，将剩余设备用于雷达。

### 排除机制

正式默认底盘为 GPIO UART，因此不会与 USB 雷达串口争抢。同一 USB-UART 仅可作为显式 `--base-port` 调试路径使用。

## 5. 现场检查命令

```bash
# 系统诊断
./bin/robot doctor

# 底盘健康检查
./bin/robot check chassis

# 雷达健康检查
./bin/robot check lidar

# TF 树检查
./bin/robot check tf

# 单独启动底盘
./bin/robot base

# 单独启动雷达
./bin/robot sensor lidar
```

## 6. 常见问题

| 问题 | 原因 | 解决方式 |
|------|------|----------|
| 串口打开失败 | 用户不在 `dialout` 组 | `sudo usermod -aG dialout robot` 后重新登录 |
| 设备名变化 | USB 口位不固定 | 雷达使用 `/dev/ydlidar` udev 别名；底盘固定 `/dev/serial0` |
| 底盘和雷达串口互换 | 手工把底盘指向 USB-UART | 恢复 `/dev/serial0`，仅调试时显式使用 `--base-port` |
| 串口被旧进程占用 | 上次运行未正常退出 | `./bin/robot stop` 后重试 |
| USB 线材不稳定 | 接触不良或线缆质量 | 更换 USB 线缆 |
