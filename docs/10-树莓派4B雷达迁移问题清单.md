# 树莓派 4B 雷达迁移问题清单

记录时间：2026-05-02 16:55 CST

本文记录从旧 x86/Ubuntu 20.04 文件系统迁移到当前树莓派 4B 环境，并尝试启动 YDLIDAR X2 雷达驱动时已经暴露的问题。

## 当前环境判断

- 当前实际运行环境：`aarch64`
- 当前系统：Ubuntu 22.04.5 LTS `jammy`
- 当前 ROS 版本：ROS 2 Humble
- 当前工作区：`/home/robot/ros2_ws`
- 当前雷达设备识别结果：
  - `/dev/ttyUSB0`
  - `/dev/serial/by-id/usb-Silicon_Labs_CP210x_USB_to_UART_Bridge_Controller_0001-if00-port0 -> ../../ttyUSB0`

## 当前阻塞问题

### 1. robot 用户没有雷达串口权限

现象：

```text
Error, cannot bind to the specified [serial port:/dev/ttyUSB0] and [baudrate:115200]
Error initializing YDLIDAR check Comms.
```

进一步检查：

```text
设备权限: crw-rw---- 1 root dialout ... /dev/ttyUSB0
当前用户组: robot adm cdrom sudo dip plugdev lpadmin lxd sambashare
```

原因：

`/dev/ttyUSB0` 属于 `root:dialout`，当前 `robot` 用户不在 `dialout` 组，因此 YDLidar SDK 无法打开串口。

状态：

- 已在脚本中加入权限预检和清晰错误提示。
- 仍需要在树莓派本机输入 sudo 密码完成系统权限配置。

处理命令：

```bash
sudo /home/robot/ros2_ws/launch_scripts/setup_lidar_permissions.sh
```

临时测试命令：

```bash
sudo chmod 666 /dev/ttyUSB0
```

注意：执行 `usermod -aG dialout robot` 后需要重新登录或重启。

## 已发现并已处理的问题

### 2. 雷达默认设备名 `/dev/ydlidar` 不存在

现象：

配置文件默认使用：

```yaml
port: /dev/ydlidar
```

但当前系统实际只有 `/dev/ttyUSB0`，没有 `/dev/ydlidar`。

处理：

- `bin/robot` 已增加自动探测逻辑：如果 `/dev/ydlidar` 不存在，会调用 `detect_lidar_port.sh` 查找实际 `/dev/ttyUSB*`。
- 新增 `setup_lidar_permissions.sh`，写入 udev 规则，长期建立稳定设备名 `/dev/ydlidar`。

涉及文件：

- `/home/robot/ros2_ws/bin/robot`
- `/home/robot/ros2_ws/launch_scripts/setup_lidar_permissions.sh`

### 3. 雷达健康检查缺少串口权限预检

现象：

原先 `check_lidar_health.sh` 会直接启动驱动，权限不足时只看到 SDK 的 `cannot bind` 错误，不容易定位到 Linux 用户组问题。

处理：

已在 `check_lidar_health.sh` 中加入：

- 串口是否存在
- 当前用户是否可读写串口
- 当前用户组
- 设备权限
- 修复命令提示

涉及文件：

- `/home/robot/ros2_ws/launch_scripts/check_lidar_health.sh`

### 4. `bin/robot mapping` 默认入口不适合当前雷达建图优先目标

现象：

原默认建图源偏向 camera，不符合“先把雷达驱动和雷达建图跑起来”的当前目标。

处理：

已将 `bin/robot mapping` 默认源改为 `lidar`。

当前推荐启动：

```bash
/home/robot/ros2_ws/bin/robot mapping --manual --real-base --base-port auto
```

手动指定雷达串口：

```bash
/home/robot/ros2_ws/bin/robot mapping --manual --real-base --base-port auto --lidar-port /dev/ttyUSB0
```

### 5. 普通沙箱中看不到真实 `/dev/ttyUSB0`

现象：

普通命令环境中查询 `/dev/ttyUSB*` 为空，但提权查看主机设备时能看到 `/dev/ttyUSB0`。

原因：

执行环境的沙箱隔离会隐藏或限制真实硬件设备访问。

处理：

涉及真实串口、`fuser`、`stty`、YDLidar 驱动启动的操作，需要在主机权限下执行。

### 6. `robot_bringup/scripts` 中部分 ROS 节点缺少可执行权限

现象：

雷达建图 launch 进入节点启动阶段后报错：

```text
executable 'scan_normalizer.py' not found on the libexec directory
'/home/robot/ros2_ws/install/robot_bringup/lib/robot_bringup'
```

原因：

`install/robot_bringup/lib/robot_bringup/scan_normalizer.py` 是指向源码目录的软链接。源码文件本身缺少可执行位时，ROS 2 launch 会认为该 executable 不可用。

处理：

已给 `src/robot_bringup/scripts/*.py` 补齐可执行权限。

验证：

短启动雷达建图时，以下节点已能正常拉起：

- `ydlidar_ros2_driver_node`
- `scan_normalizer.py`
- `static_transform_publisher`
- `fake_base_odom.py`
- `async_slam_toolbox_node`

### 7. RViz 雷达建图固定坐标系不够贴合建图场景

现象：

`lidar_mapping.rviz` 原先使用 `Fixed Frame=odom`，建图时地图显示不如 `map` 坐标直观。

处理：

已将 `src/robot_bringup/rviz/lidar_mapping.rviz` 改为：

```text
Fixed Frame: map
```

并重建 `robot_bringup`，同步到 install 目录。

### 8. SLAM Toolbox 最小激光距离参数触发边界警告

现象：

SLAM 启动时出现：

```text
minimum laser range setting (0.0 m) exceeds the capabilities of the used Lidar (0.1 m)
```

原因：

本地 SLAM 参数未显式设置 `min_laser_range`，使用了默认 `0.0`。设置为 `0.1` 时，slam_toolbox 对等于雷达下限的情况仍会提示边界警告。

处理：

已将各个雷达建图 SLAM 参数档统一设置为：

```yaml
min_laser_range: 0.09
```

涉及文件：

- `src/robot_bringup/config/slam_toolbox_mapping.yaml`
- `src/robot_bringup/config/slam_profiles/`（v0.4.0 起由 base + overlay 生成）

### 9. 真实底盘未接入时，统一建图入口应支持 fake-base 跑通雷达 RViz 建图

现象：

使用真实底盘模式时，当前只接雷达会失败：

```text
未能探测到可用的 STM32 底盘串口
```

处理：

不新增入口，继续使用统一入口 `bin/robot`。只接雷达时使用：

```bash
/home/robot/ros2_ws/bin/robot mapping lidar --manual --fake-base
```

需要真实底盘时再显式使用：

```bash
/home/robot/ros2_ws/bin/robot mapping lidar --manual --real-base --base-port auto
```

## 已发现但暂未作为雷达驱动阻塞处理的问题

### 10. `robot_localization` 安装层不完整

现象：

每次 `source install/setup.bash` 时出现：

```text
not found: "/home/robot/ros2_ws/install/robot_localization/share/robot_localization/local_setup.bash"
```

影响：

- 对当前“只启动雷达驱动并发布 `/scan`”不是直接阻塞。
- 会影响后续启用 `--ekf-base` 或依赖 `robot_localization` 的底盘融合流程。

当前状态：

历史记录：当时通过内嵌 `src/third_party/robot_localization` 修复。当前架构已改为使用 `ros-humble-robot-localization`，不再跟踪该源码。

修复内容：

- C++ 标准从旧式 C++14 构建切到 C++17。
- 将旧式 `rclcpp::Duration(0)`、`rclcpp::Duration(double)` 等写法改为 Humble 支持的 `(sec, nsec)` 或 `std::chrono` 构造。
- 为 Humble 下无默认值的 `declare_parameter` 调用补充显式 `rclcpp::ParameterType`。
- 为 `GeographicLib` 补充 CMake 查找与链接，避免 navsat 测试目标链接失败。

验证命令：

```bash
cd /home/robot/ros2_ws
source /opt/ros/humble/setup.bash
CMAKE_BUILD_PARALLEL_LEVEL=1 colcon build --symlink-install --packages-select robot_localization --parallel-workers 1
source install/setup.bash
ros2 pkg prefix robot_localization
ros2 pkg executables robot_localization
```

验证结果：

- `robot_localization` 构建安装成功。
- `source install/setup.bash` 不再出现 `robot_localization/local_setup.bash not found`。
- `ekf_node`、`ukf_node`、`robot_localization_listener_node` 可被 `ros2 pkg executables robot_localization` 识别。

### 11. ROS 2 节点在沙箱中出现 DDS socket 权限错误

现象：

在沙箱中短启动 `slam_toolbox` / `tf2_ros` 时出现：

```text
getifaddrs: Operation not permitted
TRANSPORT_UDP Error: Error creating socket: Operation not permitted
```

原因：

沙箱网络权限限制 DDS/UDP transport。

影响：

这不是 ROS 配置本身的问题。真实机器人终端中运行不应受此沙箱限制。

### 12. 系统只有 `python3`，没有 `python`

现象：

执行 skill 检查脚本时：

```text
/bin/bash: python: 未找到命令
```

处理：

已改用 `python3` 执行检查。

后续建议：

脚本中应避免硬编码 `python`，统一使用 `python3`。

## 已验证事项

### 13. `ydlidar_ros2_driver` 与 `robot_bringup` 可在当前 ARM64/Humble 环境构建

已执行：

```bash
cd /home/robot/ros2_ws
source /opt/ros/humble/setup.bash
colcon build --packages-select ydlidar_ros2_driver robot_bringup
```

结果：

```text
Summary: 2 packages finished
```

说明：

雷达驱动包和 bringup 包本身已经能在树莓派当前系统上构建通过。当前雷达不能跑起来的直接原因是串口权限，而不是驱动包编译失败。

## 当前推荐下一步

1. 在树莓派本机执行权限初始化：

```bash
sudo /home/robot/ros2_ws/launch_scripts/setup_lidar_permissions.sh
```

2. 重新登录或重启。

3. 验证雷达驱动：

```bash
/home/robot/ros2_ws/launch_scripts/check_lidar_health.sh /home/robot/ros2_ws/src/robot_sensing/config/ydlidar_x2.yaml --lidar-port /dev/ttyUSB0
```

4. 若 `/scan` 正常，再启动雷达建图：

```bash
/home/robot/ros2_ws/bin/robot mapping --manual --real-base --base-port auto --lidar-port /dev/ttyUSB0
```

5. 建图期间另开终端验收：

```bash
/home/robot/ros2_ws/bin/robot check mapping --allow-headless
```
